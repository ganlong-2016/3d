<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature android:glEsVersion="0x00030000" android:required="true" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Android 11+ 需要声明可访问的外部包（Service 启动 Client Activity 时需要） -->
    <queries>
        <package android:name="com.demo.a3ddemo.clienta" />
        <package android:name="com.demo.a3ddemo.clientb" />
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme._3ddemo">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:theme="@style/Theme._3ddemo">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- GL 渲染服务：Client App 通过 AIDL 绑定此 Service -->
        <service
            android:name="com.demo.seamless.service.GLRenderService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.demo.seamless.service.BIND" />
            </intent-filter>
        </service>
    </application>

</manifest>